export enum PageCommand {
  SaveForm = 1,
  Load = 2
}

export enum StatusType {
  Info = 1,
  Success = 2,
  Warning = 3,
  Error = 4
}

export enum Mode {
  List = 1,
  Edit = 2
}

export enum FilterType {
  Domain = 1,
  Full = 2
}

export enum Action {
  Create = 1,
  Edit = 2
}
